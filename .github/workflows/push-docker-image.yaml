# Push docker image to Docker Hub
name: Push Docker Image

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker Image
        run: docker build -t lecoursier-laravel .

      - name: Push Docker Image
        run: |
          docker tag lecoursier-laravel ${{ secrets.DOCKER_USERNAME }}/lecoursier-laravel:latest
          docker tag lecoursier-laravel ${{ secrets.DOCKER_USERNAME }}/lecoursier-laravel:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/lecoursier-laravel:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/lecoursier-laravel:${{ github.sha }}

      - name: Logout from Docker Hub
        run: docker logout
